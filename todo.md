# Power Toys — review findings

Severity: **high** the user sees something wrong or missing, or the item blocks
other work · **medium** it makes changes slow or risky, or costs the user
comfort · **low** polish, tidying, convenience.

Effort: **XS** minutes · **S** under an hour · **M** an hour or a few · **L** a day or more.

Review scope: runtime source, configuration, install/uninstall paths, policy,
udev rules, styles, icons and documentation. Cache, build output, tests and
test-only tooling were excluded as requested.

Review categories: functional correctness; reliability, lifecycle and
concurrency; security and permissions; performance and resource use; UX and
accessibility; internationalization; compatibility and maintainability;
architecture and SOLID; coupling; wiring and integration; state management;
installation and operations; testing and quality assurance; documentation and
assets. Each item names its primary category even where the impact crosses
categories.

## Findings

| id | category | status | effort | severity | description |
| --- | --- | --- | --- | --- | --- |
| PT-300 | Reliability, lifecycle and concurrency | rejected/won't fix | S | low | During screenshot-assisted live verification Cinnamon emitted one unattributed `Clutter-WARNING`: an `StIcon` was removed from a `ClutterBox` that was not its parent. Power Toys does not directly remove either of its `StIcon` actors, five isolated menu open/close cycles plus delayed-callback time emitted no log line, and three applet reloads were also clean. With no stack, reproduction, or Power Toys attribution, a runtime change would be speculative; revisit only if an applet-only reproduction identifies the owning actor. |
| PT-306 | Compatibility and maintainability | rejected/won't fix | XS | medium | Sonar recommends newer built-ins including `Object.hasOwn`, `Array.at`, and `Array.toSorted`. The applet's Cinnamon 5.4 floor uses Mozilla JavaScript 78, which predates the first two; `toSorted` is absent even from the installed CJS 115. These style-only rewrites are not applicable to the supported runtime range. |
| PT-307 | Reliability, lifecycle and concurrency | rejected/won't fix | XS | low | Sonar reports documented empty catches in best-effort Cinnamon compatibility, cleanup, cancellation, and rollback paths. Those operations deliberately continue after an optional restoration or release fails, and turning them into user-visible errors or aborts would make cleanup less reliable; retain the contained fallback behavior. |
| PT-308 | Security and permissions | rejected/won't fix | XS | low | Sonar's Python path rules treat the three explicit operands of `tools/build-policy.py` as an LLM-controlled filesystem boundary. The script is a non-privileged build CLI invoked with trusted repository, translation-directory, and temporary-output paths; a caller able to choose different paths already has exactly the same filesystem permissions as the script, so validating them against an invented root would change the tool's intended interface without adding a security boundary. |
| PT-312 | Testing and quality assurance | rejected/won't fix | XS | low | The clipped-note accessibility test searches `applet.js` for the exact call text `this.actor.set_accessible_name(text)` instead of exercising the row. A semantics-preserving local rename caused the otherwise unrelated assertion to fail; tests are outside this review's requested scope, so retain the harmless local name rather than expanding this Sonar cleanup into a test-harness rewrite. |
| PT-313 | Reliability, lifecycle and concurrency | rejected/won't fix | XS | low | Sonar recommends combining the two UPower manager signal IDs into one `Array.push` call. The current calls intentionally publish the first ID before attempting the second so a throw during the second subscription can disconnect the first; evaluating both as arguments before `push` would lose that rollback handle and leak the signal. |
| PT-357 | Testing and quality assurance | open | M | medium | Add reproducible semantic linting to `make check`: a CJS-5.4-compatible ESLint configuration with GJS/Cinnamon globals, ShellCheck for shipped and installation scripts, and a lightweight Python lint gate for developer tooling, with narrowly documented compatibility exceptions. |
| PT-358 | Testing and quality assurance | open | M | medium | Run the deterministic, lossless fail-fast mutation command in automation, either as a bounded pull-request job or a scheduled full job; enforce the configured threshold and retain its report as an artifact. The current workflow runs check and coverage but not `make mutants`. |
| PT-368 | UX and accessibility | open | S | low | The responsive menu arrangement has not been seen on a running session. `lib/menu-layout.js` and its application in `ui/menu.js` are covered by cases, but the three shapes were never drawn: confirm on a live Cinnamon that two-column and one-column arrangements look right (the `.powertoys-panel-stacked` rule above a column that begins a new row, and the column min-width against a narrow work area), that Tab still walks the rows in the order they are drawn, and that a menu taller than the work area still scrolls. The measurements in `_menuConstraints` are guarded, so a shell that will not answer one of them falls back to a single column rather than failing; that fallback is also unseen. |
| PT-369 | Reliability, lifecycle and concurrency | open | S | medium | `lib/ddc.js` cancels queued ddcutil jobs on teardown but never the running one. `runCommand` (`70-135`) arms an eight second `GLib.timeout_add` and a `communicate_utf8_async` and hands back no handle, so `destroy()` (`989-1002`) and `stop()` (`593-617`) can only drain `_commandQueue` through `_cancelQueuedCommands`; `_activeCommand` runs on. After the applet is removed or reloaded mid-probe - and the open menu probes once a second - a timeout source stays armed for up to eight seconds and an orphan `ddcutil` keeps the I2C bus, against the `CALL_TIMEOUT_MS` comment at `32-34` which says exactly that must not happen. Give `runCommand` a `{ cancel() }` that removes the timer, cancels a `Gio.Cancellable` passed to `communicate_utf8_async` and force-exits the child; keep it on `_activeCommand` and call it from both `destroy()` and `stop()`. |
| PT-373 | Security and permissions | open | XS | low | `powertoys-helper:171-172` and `225-226` expand the kernel's advertised list unquoted - `contains "$policy_value" $policy_available` - which enables pathname expansion as well as the intended word splitting. Verified: with a list containing `*` and a matching filename in the working directory, `contains` returns 0 for a value the kernel never offered, and pkexec does not chdir so the working directory is the caller's. No kernel advertises a glob character in `scaling_available_governors`, `energy_performance_available_preferences` or `platform_profile_choices` today, so this is not exploitable - but the validation of a root-owned write rests on that. Bracket the two expansions with `set -f` / `set +f`. |
| PT-376 | Compatibility and maintainability | open | XS | low | `udev/99-cinnamon-powertoys-rapl.rules:23-25` names `/usr/bin/chgrp` and `/usr/bin/chmod`, and udev's `RUN+=` does no `PATH` lookup. On a system where coreutils is in `/bin` both runs fail, udev logs it, and the user sees only a missing package-power row after `sudo make install-rapl` printed success. `tools/rapl-access.sh` already templates the rule with `sed`; resolve `@CHGRP@`/`@CHMOD@` with `command -v` at install time and fail the install when either is absent. |
| PT-377 | Installation and operations | open | XS | low | `tools/rapl-access.sh:45-50` validates the group name as characters only; the `getent group` liveness check lives in `Makefile:131-132` alone. Run directly with a group that does not exist, the script templates a rule that fails at every event - and with `0`, `chgrp` resolves GID 0 and the grant silently becomes root-only while the install reports success. Move the `getent` check into `install_rule`, guarded by `[ -z "${DESTDIR:-}" ]`, and drop the Makefile copy. |
| PT-378 | Documentation and assets | open | XS | low | `README.md:322-324` and `419-420` say the helper "validates every value against the list the kernel advertises" and offers "five fixed commands". Three commands are checked against a kernel list; `boost` (`powertoys-helper:200-203`) is checked against a hardcoded `0|1` and `charge-threshold` (`232-237`) against a hardcoded 20-100 that is the applet's policy rather than the kernel's, and the helper accepts six commands - `protocol-version` (`304-308`) is the sixth. The writes are still bounded, so this is accuracy and not a hole. Reword to "against the list the kernel advertises where there is one, and against a fixed range otherwise", and count six. |
| PT-379 | Security and permissions | blocked | S | medium | `powertoys-helper:262-290` silently rewrites `charge_control_start_threshold` to `value - 5` (floored at 0) whenever the existing start threshold is at or above the requested end threshold, and restores it only if the transaction fails - so on success a node TLP and vendor utilities commonly own is permanently changed, with nothing in the README, the polkit `<message>` or the applet's change notification saying so. A decision is needed before anything is written: (a) keep the coercion and document it in README's permissions section and in the polkit message the user reads at the password prompt; (b) refuse with `die invalid-value "the start threshold must be lowered first"` and leave the choice to the user; or (c) keep it and report the adjustment back so `Reading.describeChange` can name it in the notification. Each changes what the user is told they agreed to, so it is not ours to pick. |
| PT-380 | Architecture and SOLID | open | L | medium | `lib/cpu.js` and `lib/power-supply.js` each ship two implementations of the same rules and the applet only ever constructs the asynchronous one (`applet.js:105-119` passes `asynchronous: true` unconditionally and there is no other production constructor). Dead in the shipped applet, live in the suite: `cpu.js:149-191` `_refreshSync` against `_stateFrom` (`235-275`), `cpu.js:52-59` `_agreed` against the identical closure at `291-298`, `cpu.js:433-449` `averageFrequency` against the same loop at `299-320`, `cpu.js:27-42` `_lazy` reachable only through the sync branch of `snapshot()`; `power-supply.js:65-97` `ChargeControl` and `255-267` `discoverChargeControl` against `AsyncChargeControl._startRefresh` (`147-175`), and `270-277` `platformProfile()` against the parse in `refresh()` (`332-337`). A rule corrected in one half is not corrected in the other and the suite still passes. Delete the synchronous halves and the `asynchronous` option, and point the cases at the async classes through the `IO.setRoot` and injected-`fileFactory` seams they already have. |
| PT-381 | Documentation and assets | open | XS | low | Two comment blocks describe the dead synchronous path as if it were what ships. `lib/cpu.js:486-498` explains that governor, energy preference, boost and frequency "are worked out when somebody asks" so "a poll now reads no cpufreq node at all" - that documents `_lazy`, and in the shipped asynchronous mode `snapshot()` returns at `483` with values up to one poll old. `lib/power-supply.js:425-433` describes `_read()` collapsing six property reads into one, which is true of the sync `_read()` and not of the async field access at `305-307`. Remove both with the code in PT-380. |
| PT-382 | Internationalization | open | S | low | One "label: value" join is six separate msgids for a translator to keep consistent, and English already renders one of them differently. `lib/format.js:29-36` (`%{label}: %{value}`) states outright that this join "lives here rather than in each row"; `lib/panel-text.js:182-186`, `264-267`, `294-297` and `307-311` and `lib/alerts.js:36-39` each reimplement it, the last with a dash instead of a colon. Route all six through `Format.readingName` and drop the five extra entries from the template. |
| PT-383 | Architecture and SOLID | open | M | medium | Four copies of one capped-doubling retry timer: `lib/upower.js:511-544`, `lib/bluez.js:618-638`, `lib/backlight.js:347-378` and `lib/profiles.js:491-517`, with `lib/owner-watch.js:81-91` a fifth at different bounds. Each is the same "already armed / destroyed / owner absent" guard around the same doubling delay, differing only in the injected timer shape and the may-I-run predicate, and `profiles.js` additionally writes the `timeoutAdd ? ... : GLib.timeout_add` fallback pair out twice (`279-285` and `503-513`). A correction to the backoff has to be made five times. Extract one `lib/backoff.js` taking `{ timers, initialMs, maxMs, allow, run }` with `schedule()`/`cancel()`, and delete the copies. |
| PT-384 | Architecture and SOLID | open | S | low | The exactly-once-settle guard is hand-rolled at least seven times: `lib/backlight.js:498-506` (`_once`, the only named one), `lib/profiles.js:658-666` (a second `_once`), `lib/ddc.js:76-93` and `649-662`, `lib/upower.js:550-558`, `lib/sensors.js:1384-1390`, `lib/io.js:70-97` and `lib/pending-profile.js:88-106`. Each re-derives the same rule, and the unnamed ones are where an exactly-once contract is easiest to break without anything saying so. Export one `once(fn)` from a small shared module and use it at all of them. |
| PT-385 | Architecture and SOLID | open | M | medium | `PowerProfilesClient` (`lib/profiles.js:233-720`) is 490 lines holding two D-Bus name watches, backend selection and priority, proxy construction with cancellation, a connect-retry timer, a serialised profile-write queue with supersede semantics, a snapshot cache and failure-log keying - thirteen private lifecycle fields whose invariants can only be read by reading all of it. Split into `ProfileBackendConnection` (watches, selection, proxy, retry), `ProfileWriteQueue` (`640-701`) and a thin facade holding the snapshot. |
| PT-386 | Architecture and SOLID | open | M | medium | `SensorSet` (`lib/sensors.js:1019-1477`) is 458 lines carrying two independent coalescing state machines (`_discovering`/`_discoverAgain`/`_discoverWaiters` at `1055-1093` and `_refreshing`/`_refreshPending`/`_refreshWaiters` at `1128-1184`), a topology-change detector, a path collector, a reading assembler, a sensor-selection policy (`1208-1223`) and an I/O scope owner - plus two execution modes whose contracts differ, since `refresh(onDone)` silently drops `onDone` on the synchronous branch (`1131-1136`). Split into a `SensorInventory` (discovery, topology, coalescing) and a `SensorReader` that is a pure function of an inventory and a value map. |
| PT-387 | Architecture and SOLID | open | M | medium | `DdcBacklight` (`lib/ddc.js:509-1003`) mixes a serialising command scheduler with write-priority insertion (`629-681`), a topology detector with confirmation and failure-grace policy (`792-888`), a monitor-collection lifecycle (`769-790`) and the group control surface. `_detect` alone (`822-888`) is 66 lines with five exit paths and its own callback-token protocol. Lift the scheduler out as a `CommandQueue` that knows nothing about DDC - which is also where PT-369's cancellation belongs - and split `_detect` into transport-plus-staleness and policy halves. |
| PT-388 | Architecture and SOLID | open | M | medium | `PrivilegedHelper` (`lib/privileged.js:216-432`) holds five responsibilities - candidate trust inspection, protocol probing and the candidate walk, job queueing, pkexec argv construction and spawning, and stderr-to-stable-code translation - and takes four injectable collaborators to compensate. `_next()` (`370-417`) does six of those things across three callback levels with `this._running = false` reset in three separate branches. Split a `HelperLocator` (candidates, inspect, probe, `select(onDone)`) out of the queue-and-spawn half, and give the remainder one `_finishJob(job, outcome)` that owns the `_running` / `job.done` / `_next()` sequence. |
| PT-389 | Reliability, lifecycle and concurrency | open | XS | low | `lib/backlight.js:322-328` is the one path in the module that calls the injected `_onChanged` without checking `destroyed`. `destroy()` sets the flag and then `_dropProxy()` -> `_cancelReads()` (`518-528`) deliberately settles every pending read, so tearing down while the `GetPercentage` started by `_onOwnerAppeared` is in flight runs the applet's change handler after the applet has gone. `_settleReady` guards itself and `_scheduleRetry`'s callback guards at `353-359`, so this is the odd one out rather than a live crash - the applet's own `_destroyed` check absorbs it - but the module must not depend on its caller for that. Add the guard, and neutralise `_onChanged` in `destroy()` the way `SensorSet.destroy` does. |
| PT-390 | Reliability, lifecycle and concurrency | open | S | medium | In `lib/upower.js:643-664` a `DeviceAdded` proxy that errors, or whose `g-properties-changed` connect fails, sets `available = false` and calls `_scheduleRetry()` - and that retry (`511-533`) runs `_disconnectManager()`, which bumps the generation and drops the manager, the display device and every device proxy before rebuilding from scratch. The ordinary trigger is benign: a bluetooth peripheral that disconnects between `DeviceAdded` and the proxy reply. So one peripheral flickering blanks the whole battery list and rebuilds every proxy. In the `!done` branch drop only that path and report it under a per-path failure key; keep `available = false` and the retry for manager-level failures, which is what `_onManagerReady` and `_onEnumerated` already do. |
| PT-391 | Reliability, lifecycle and concurrency | open | XS | low | `PanelAdapter._disconnectHover()` (`lib/cinnamon-panel.js:267-277`) disconnects each handler but never clears `this._hoverActor`, and `destroy()` (`279-292`) clears `_hoverSignals` and `_applet` but not that field - so the adapter outlives the applet holding a live reference to the panel actor Cinnamon is tearing down, and a second `_disconnectHover()` walks an emptied list against a dead actor. Null `_hoverActor` at the end of `_disconnectHover()`, which also makes it idempotent the way `_restoreTooltipHooks` already is. |
| PT-392 | Compatibility and maintainability | open | XS | low | Two exports have no runtime caller and are kept alive only by their own cases: `Format.reportsPrecisePercentage` (`lib/format.js:260-262`), where every site uses `Format.batteryReading(device).precise`, and `IO.isReadable` (`lib/io.js:334-336`), whose implementation - open the node and read all of it - is precisely what the comment above `canRead` at `338-341` says must not be done to a sysfs node. Delete both with their cases. |
| PT-393 | Functional correctness | open | XS | low | `lib/sensors.js:1246-1247` records "this fan has spun" by setting `hasRun` on the discovered sensor record, and `_adopt` (`1095-1105`) replaces `this.fanSensors` wholesale with freshly built records on every rediscovery that finds a changed topology. So an unlabelled fan that has spun and then stopped loses its `inUse` status and drops out of the menu, against the comment at `1258-1260` which says it "keeps it when the fan later stops". Hold `hasRun` in a set of sensor ids on `SensorSet` that survives `_adopt`, and leave the discovered records immutable. |
| PT-394 | Documentation and assets | open | XS | low | `lib/ddc.js:50-52` says command failures "get one extra chance", but `FAILURE_GRACE` is 3 and the test at `852-854` is `++_detectFailures >= FAILURE_GRACE`, so the topology survives the first and second failures and is cleared on the third - two extra chances. Correct the comment or the constant; the code is the source of truth for which. |
| PT-395 | Architecture and SOLID | open | S | low | `lib/sensors.js:865-893` (`topologyKeyAsync`) and `897-938` (`discoverSnapshotAsync`) are the same function twice: the same `_directoryInventoryAsync`, the same three loads with the same `!/\/(?:energy_uj|power_uw)$/` filter, the same hand-rolled tri-null barrier and the same read/readLink/readable adapters. `discoverSnapshotAsync` already produces the topology at `919`, yet `_startRefresh` (`1154-1167`) sweeps sysfs a second time purely to compute a key the next call recomputes. Extract one `_loadInventoryAsync` both are built on, and let `refresh` reuse the topology from the last snapshot. |
| PT-396 | Architecture and SOLID | open | S | low | The "append an identity to names that collide" algorithm is written three times: `lib/ddc.js:207-224` (`nameDisplays`), `lib/sensors.js:270-288` (`_finalizeNames`) and `lib/sensors.js:319-335` (`_nameGroupsFrom`). Each maps to a base name, counts by that name, then re-maps appending `" (" + identity + ")"` where the count exceeds one. One `disambiguate(items, { name, key, identity })` helper turns all three into single calls. |
| PT-397 | Architecture and SOLID | open | M | medium | `PowerToysApplet` (`applet.js:196-2081`) is 1,885 lines, 71 methods and roughly thirty mutable fields, and the split into `ui/` moved presentation out without reducing what the coordinator owns: settings binding, migration and normalisation; the monitor-probe state machine (`592-743`); reading collection and assembly (`900-1199`); profile-backend selection and ownership generations; privileged-helper policy and its user-facing vocabulary; wheel, middle-click and hotkey input; and shell measurement. `_initialize` alone is 168 lines (`237-405`), and `_teardown` (`2010-2081`) has to know every one of those fields by name. Split the concerns that are already pure - see PT-398, PT-399 and PT-400 - out into `lib/`, where the gates can reach them, before splitting the rest. |
| PT-398 | Architecture and SOLID | open | S | medium | `applet.js:1292-1355` - `_menuConstraints`, `_workAreaWidth`, `_scaleFactor` and `_textScale` - is sixty lines of pure derivation over three shell interfaces, each probed defensively with a fallback, and it is the sole input to the responsive arrangement PT-368 says has never been drawn. Because it sits in `applet.js` it is outside both gates, so no fallback in it - a shell with no `findMonitorForActor`, no `getWorkAreaForMonitor`, or no `org.cinnamon.desktop.interface` schema - has ever been executed by anything. Move it to `lib/shell-metrics.js` taking the layout manager, the global and Gio as parameters, leave the applet a one-line call, and cover each fallback. |
| PT-399 | Architecture and SOLID | open | S | low | The wheel-gathering state machine is written twice: `applet.js:1880-1897` with `_cancelPendingScroll` at `1938-1943`, and `ui/controls.js:511-528` inside `BacklightSlider._onScrollEvent`. Both accumulate into a pending total, restart a `SCROLL_SETTLE_MS` timeout, round through `Controls.settledScrollSteps` and clear the timer on teardown; the constant and the rounding are already shared, the state machine around them is not. Extract one `ScrollGatherer` taking an injected timer into `lib/`, where it can be covered and mutated, and have both use it. |
| PT-400 | Architecture and SOLID | open | S | low | `applet.js:1615-1635` (`_runHelperQuietly`) and `1772-1834` (`_runHelper`) share a spine - the privileged-controls gate with the same sentence, closing the menu for the password dialog, running the helper, the `_destroyed` guard, the stale-helper warning, `_cpu.refresh()` and `_update()` - and differ only in how the outcome is reported and in `_runHelper`'s extra `_scheduleUpdate()`. They have already drifted: `_runHelperQuietly:1617` calls `onDone` unguarded where `_runHelper:1781` treats it as optional. Make one `_callHelper(args, report, onDone)` and let the caller supply the reporting. |
| PT-402 | Reliability, lifecycle and concurrency | open | XS | low | `PrivilegedHelper.path()` (`lib/privileged.js:277-286`) is public and overwrites `this._activeProbe` (`304-305`) without cancelling the previous one, so a second call while a probe is outstanding orphans that probe's `Gio.Subprocess`, its pipes and its two-second timer beyond the reach of `destroy()`. `_next()` serialises real jobs so nothing in the applet does this today, but the method is exported. It also always returns null while being named like a getter. Rename it `_selectHelper(onDone)`, drop the dead `return null`, and cancel any outstanding probe on entry. |
| PT-403 | Architecture and SOLID | open | M | low | The same stage / back up / publish under a blocked signal window / roll back state machine is written three times: `tools/install-policy.sh:47-175` (uninstall), `182-303` (install) and `tools/rapl-access.sh:40-116` (install), differing only in variable-name prefix - roughly 250 of those 472 lines. The reasoning about which commands the `trap '' HUP INT TERM` window must cover has to be re-applied correctly in each. Extract a `tools/atomic-replace.sh` sourced beside `transition-lock.sh` offering stage, publish, rollback and discard over a caller-supplied slot name; each script then expresses only its own ordering. |
| PT-404 | Architecture and SOLID | open | S | low | Three duplications and two overlong functions in `powertoys-helper`. `write_node` (`86-93`) is `try_write` plus a `die`, with the explanatory comment on only one of the two; the "append a record to the plan, with a newline if the plan is not empty" block is written out at `176-182`, `283-289` and `291-297` under three variable names; `set_charge_threshold` (`230-302`, 73 lines) does range validation, battery enumeration, type filtering, readability checks, old-value validation, the start-threshold coercion rule, plan assembly and apply; `write_policies` (`139-187`) does the same for cpufreq. Reduce `write_node` to `try_write || die`, add one `plan_append`, and pull `discover_batteries`, `validate_threshold` and `validate_policy_node` out so both read as plan-then-apply. |
| PT-405 | Reliability, lifecycle and concurrency | open | S | low | `lib/backlight.js` uses one `_valueGeneration` for two purposes, and the comment at `544-546` describes only one of them: it says "a later mutation owns the next visible value", but `_startRead` (`430`) and `_readPercentage` (`410`) bump the same counter. So the daemon's own `Changed` signal - which our own `SetPercentage` provokes - runs `refresh()` and invalidates the still-pending `SetPercentageRemote` reply, which is then settled as `{ ok: false, cancelled: true }` at `586-590` although the write succeeded. The visible value recovers from the read so today's caller (`ui/controls.js:466`) survives, but the documented outcome contract is wrong for the commonest ordering. Split the counters: a read generation for read coalescing, and `_valueGeneration` moved only by the mutation queue. |
| PT-406 | Architecture and SOLID | open | S | low | `lib/backlight.js:558-649` `_drainMutations` is 65 lines holding a 40-line `finish` closure with four mutually exclusive outcome branches, dispatch over three operation types, and `_runSteps` as a second nested state machine sharing four captured generation variables; its failure branch (`593-609`) re-enters `_readPercentage` while the operation is still queue owner, so that ownership invariant is spread across two functions. Extract the three settle paths and give `_runSteps` an object of its own. |
| PT-407 | Architecture and SOLID | open | S | low | `lib/upower.js:88-173` builds menu-row shapes - labels, `groupLabel`, `shortLabel`, kind strings - in `sensorReadings`, and encodes display ordering in `reportedDevices`, inside a module whose stated job (header `1-7`) is talking to `org.freedesktop.UPower`; the comment at `74-87` already notes they duplicate the shape `lib/sensors.js` produces. Both are pure functions of a device list. Move them beside the other presentation code and let `read()` (`796-810`) return devices. |
| PT-408 | Coupling | open | XS | low | `lib/reading.js:17-19` requires the whole of `lib/power-supply.js` for one string constant (`PLATFORM_BACKEND`, used at `111-120`) and the whole of `lib/sensors.js` - 1,477 lines - for the `sensorMatches` predicate (used at `182` and `190`). A pure derivation module taking a hard dependency on two backend modules inverts the intended direction and drags both into anything that loads a reading. Move the constant somewhere shared and pass the matcher in. |
| PT-409 | Architecture and SOLID | open | S | low | Three incompatible port shapes for one dependency: `ResilientOwnerWatch` is wired with timers read off `this._bus.timeoutAdd/removeTimer` in `lib/upower.js:256-274`, off `this._timers.timeout_add/source_remove` in `lib/bluez.js:226-240`, and off `this._owner.timeoutAdd/removeTimer` in `lib/backlight.js:190-210` - same options object, same failures/failureKey/failureMessage triple, three conventions. That is a dependency-inversion violation that stops the three backends being exercised through one fake. Define the timer port once in `lib/owner-watch.js`, export a `watchOwnership` factory, and have all three call it. |
| PT-410 | Testing and quality assurance | open | XS | low | `lib/privileged.js:229-231` reads `this._probe = probe \|\| (spawn ? ((path, onDone) => onDone(true, "")) : _probeHelper)`, so supplying a custom `spawn` - a legitimate integration seam, not only a test hook - silently turns the helper protocol handshake into an unconditional yes. Default `probe` to `_probeHelper` regardless and let a caller inject the stub it actually wants. |
| PT-411 | Reliability, lifecycle and concurrency | open | XS | low | `CpuControl.destroy()` (`lib/cpu.js:506-527`) ends with `if (firstError) throw firstError`, the only `destroy()` in `lib/` that can throw - `power-supply.js:238-247`, `profiles.js:707-719`, `cinnamon-panel.js:279-292` and `panel-presenter.js:136-140` all cannot. Nothing is stranded inside the class and `applet.js`'s `release()` wrapper absorbs it, but the contract is invisible from the call site and a caller tearing two backends down in sequence loses the second. Log the waiter errors instead, as `lib/notifications.js:19-22` and `lib/alerts.js:80-83` already do. |
| PT-412 | Architecture and SOLID | open | XS | low | `lib/power-supply.js:336` (`rawChoices ? rawChoices.split(/\s+/) : []`) and `lib/cpu.js:237-240` (`words()`) each re-derive what `IO.readWords` (`lib/io.js:241-246`) does, and the reasoning for the leading-empty-string case is spelled out at `lib/io.js:232-240` and repeated at neither. Export an `IO.toWords(raw)` beside `IO.toNumber` - which exists for exactly this reason, see its comment at `186-191` - and have all three use it. |
